using UnityEngine;
using System.Collections;

public class UIStart : MonoBehaviour {

    void Start()
    {
        UIManager uiManager = new UIManager();
        UIPanelManager uiPanelManager = new UIPanelManager();
    }
}
